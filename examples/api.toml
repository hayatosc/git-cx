provider   = "api"
model      = "gpt-5"
candidates = 3
timeout    = 30

api_base_url = "https://api.openai.com/v1"

[commit]
use_emoji          = false
max_subject_length = 100
# scopes = ["feat", "fix", "docs", "refactor"]
